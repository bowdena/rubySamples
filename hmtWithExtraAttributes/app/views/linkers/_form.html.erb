<%= form_with(model: linker, local: true) do |form| %>
  <% if linker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(linker.errors.count, "error") %> prohibited this linker from being saved:</h2>

      <ul>
      <% linker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :topic_id %>
    <%= form.text_field :topic_id, id: :linker_topic_id %>
  </div>

  <div class="field">
    <%= form.label :article_id %>
    <%= form.text_field :article_id, id: :linker_article_id %>
  </div>

  <div class="field">
    <%= form.label :relevance %>
    <%= form.text_field :relevance, id: :linker_relevance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
